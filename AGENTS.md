# 项目规则

1. 先想再写：编码前梳理逻辑、需求假设，有歧义主动确认，理清方案再动笔～
2. 易维护优先：在满足需求的前提下保持实现清晰、结构合理，避免多余功能和冗余抽象；关键逻辑、复杂分支、异常处理需添加详细注释，重要流程、错误场景和外部依赖调用需补充必要日志，方便后续排查和维护。
3. 精准改动：只改和需求直接相关的代码，不擅自动无关部分，改动范围贴合目标
4. 目标驱动：全程围绕业务核心目标写代码，明确验证标准，不偏离需求核心啦
5. 服务器开发：尽量按照 go-zero 的开发规范进行开发；涉及 `.api`、handler、types、svc、model 等能由 goctl 自动生成的代码，优先使用 goctl 生成，不手写可生成代码；数据库表对应的 `model` 代码应通过 goctl model 生成，并以表结构或 schema 作为来源。
6. 文档语言：以后所有实施计划、设计文档、产品文档必须使用中文撰写；涉及代码标识、文件路径、接口名、命令等可保留英文原文。



## Superpowers 执行策略

本项目默认使用 FAST 模式。任务没有明确指定模式时，根据风险自动选择，但优先选择成本更低的模式。

### FAST 模式

适用于以下条件全部成立的任务：

* 修改范围预计不超过 2 个生产代码文件；
* 不涉及数据库结构、公共 API、库存、订单、支付、权限、并发或数据迁移；
* 问题表现明确，能够通过现有日志、错误信息或代码快速定位；
* 不属于架构设计或大规模重构。

FAST 模式必须遵守：

1. 不使用 brainstorming。
2. 不编写设计文档和 implementation plan。
3. 不创建 git worktree。
4. 不使用 subagent-driven-development。
5. 不派遣 implementer、reviewer 或其他子代理。
6. 不进行全代码库搜索，除非定向搜索无法找到原因。
7. 先快速确认根因，再进行最小修改。
8. 优先运行已有的定向测试。
9. 没有合适测试基础时，不为了一个简单修复大规模重构代码；可以使用可重复的手动验证、静态分析或最小测试脚本。
10. 默认只运行受影响文件或模块的测试和分析，不运行完整测试集。
11. 不进行与当前问题无关的重构或代码清理。
12. 完成后只汇报根因、修改文件、验证命令和验证结果。

### STANDARD 模式

适用于普通业务 Bug、多文件联动和存在一定回归风险的修改。

STANDARD 模式必须遵守：

1. 使用简化的 systematic-debugging。
2. 调查错误信息、调用路径和相关改动。
3. 形成一个明确的根因假设。
4. 优先添加最小回归测试。
5. 只修改解决根因所必需的代码。
6. 运行目标测试和受影响模块测试。
7. 默认不使用 brainstorming、writing-plans、git worktree 或子代理。
8. 只有发现跨模块、高风险或架构问题时，才升级为 FULL 模式。

### FULL 模式

仅在以下任务中使用完整 Superpowers 流程：

* 库存、订单、支付、退款和财务数据；
* 数据库结构或数据迁移；
* 权限、认证和安全；
* 并发、事务和数据一致性；
* 公共 API 或多个客户端共用接口；
* 大规模重构或架构设计；
* 修改范围较大且无法可靠地进行局部验证。

FULL 模式可以使用：

* brainstorming；
* writing-plans；
* test-driven-development；
* git worktree；
* subagent-driven-development；
* 代码审查和最终分支审查。

### 模式升级规则

执行 FAST 或 STANDARD 模式时，只有发现以下情况才能升级：

* 实际修改范围明显超出预计；
* 根因涉及多个模块；
* 修改可能影响核心业务数据；
* 无法通过定向测试可靠验证；
* 连续两个最小修复假设均被证伪。

升级前说明升级原因，不要因为任务存在少量不确定性就自动使用完整流程。

### Token 与上下文控制

1. 不重复读取已经读取过且没有变化的文件。
2. 不把完整日志、完整 diff 或完整计划反复复制进上下文。
3. 优先通过文件路径向子代理传递内容。
4. 简单任务使用当前会话直接完成。
5. 只有独立且复杂的任务才使用子代理。
6. 默认采用能够完成任务的最低合理推理强度。
7. 不为了形式上的流程完整而增加无助于正确性的步骤。
8. 对已经验证通过的步骤，不重复运行相同命令，除非代码发生了相关变化。
